const RC={money(n,digits=0){const v=Number(n);return Number.isFinite(v)?v.toLocaleString('en-US',{style:'currency',currency:'USD',minimumFractionDigits:digits,maximumFractionDigits:digits}):'—'},num(id,fallback=0){const el=document.getElementById(id);const v=el?Number(el.value):NaN;return Number.isFinite(v)?v:fallback},clamp(v,min,max){return Math.min(max,Math.max(min,v))},show(id,html){const el=document.getElementById(id);if(!el)return;el.innerHTML=html;el.classList.add('show')},rows(rows){return '<table>'+rows.map(r=>'<tr><td>'+r[0]+'</td><td>'+r[1]+'</td></tr>').join('')+'</table>'},validPositive(v){return Number.isFinite(v)&&v>=0}};